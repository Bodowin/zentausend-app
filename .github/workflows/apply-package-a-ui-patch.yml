name: Apply package A UI patch

on:
  push:
    branches:
      - chatgpt/package-a-state-safety

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: chatgpt/package-a-state-safety

      - name: Apply guarded patch
        run: node scripts/apply-package-a-ui-patch.mjs

      - name: Remove one-time patch machinery
        run: git rm scripts/apply-package-a-ui-patch.mjs .github/workflows/apply-package-a-ui-patch.yml

      - name: Commit patched source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/GameScreen.tsx
          git commit -m "Fix virtual mode toggle and last-chance score"
          git push origin HEAD:chatgpt/package-a-state-safety
