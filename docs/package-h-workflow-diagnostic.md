# Paket H – Workflow-Diagnose

Patch: success
Unit: failure
Build: skipped
Chromium: skipped
E2E: skipped

## package-h-patch.log
```text
Family sync, optional haptics and natural-dice roadmap applied.
```

## package-h-unit.log
```text
[31m+             onChange={(e) => setB(e.target.value)}[39m
[31m+             className="w-full rounded-xl border border-ink-700 bg-ink-900 px-2.5 py-2 text-right text-sm font-bold text-mint-400"[39m
[31m+           >[39m
[31m+             {players.map((player) => ([39m
[31m+               <option key={player.id} value={player.id} disabled={player.id === aId}>[39m
[31m+                 {player.name}[39m
[31m+               </option>[39m
[31m+             ))}[39m
[31m+           </select>[39m
[31m+         </div>[39m
[31m+[39m
[31m+         {h.games === 0 ? ([39m
[31m+           <p className="py-3 text-center text-xs text-fog-500">Noch kein gemeinsames Spiel.</p>[39m
[31m+         ) : ([39m
[31m+           <>[39m
[31m+             <div className="mb-1 flex items-center justify-between text-xs font-bold">[39m
[31m+               <span className="text-gold-400">{h.aAhead}×</span>[39m
[31m+               <span className="text-fog-600">öfter vorn</span>[39m
[31m+               <span className="text-mint-400">{h.bAhead}×</span>[39m
[31m+             </div>[39m
[31m+             <div className="mb-4 flex h-2.5 overflow-hidden rounded-full bg-ink-900">[39m
[31m+               <div className="bg-gold-500/70" style={{ width: `${aPct}%` }} />[39m
[31m+               <div className="bg-mint-400/70" style={{ width: `${100 - aPct}%` }} />[39m
[31m+             </div>[39m
[31m+[39m
[31m+             <DuelRow label="Spiele zusammen" a={h.games} b={h.games} same />[39m
[31m+             <DuelRow label="Siege" a={h.aWins} b={h.bWins} />[39m
[31m+             <DuelRow label="Bestwert" a={fmt(h.aBest)} b={fmt(h.bBest)} cmp={[h.aBest, h.bBest]} />[39m
[31m+             <DuelRow label="Ø Punkte" a={fmt(h.aAvg)} b={fmt(h.bAvg)} cmp={[h.aAvg, h.bAvg]} />[39m
[31m+[39m
[31m+             {(nemA || nemB) && ([39m
[31m+               <div className="mt-3 border-t border-ink-800 pt-3 text-[11px] text-fog-500">[39m
[31m+                 {nemA && ([39m
[31m+                   <p>[39m
[31m+                     😨 <span className="font-semibold text-fog-300">{aPlayer.name}</span>s Angstgegner:{' '}[39m
[31m+                     <span className="font-semibold text-coral-400">{nemA.name}</span> ({nemA.ahead}/{nemA.of} vorn)[39m
[31m+                   </p>[39m
[31m+                 )}[39m
[31m+                 {nemB && ([39m
[31m+                   <p className="mt-0.5">[39m
[31m+                     😨 <span className="font-semibold text-fog-300">{bPlayer.name}</span>s Angstgegner:{' '}[39m
[31m+                     <span className="font-semibold text-coral-400">{nemB.name}</span> ({nemB.ahead}/{nemB.of} vorn)[39m
[31m+                   </p>[39m
[31m+                 )}[39m
[31m+               </div>[39m
[31m+             )}[39m
[31m+           </>[39m
[31m+         )}[39m
[31m+       </div>[39m
[31m+     </section>[39m
[31m+   )[39m
[31m+ }[39m
[31m+[39m
[31m+ function DuelRow({[39m
[31m+   label,[39m
[31m+   a,[39m
[31m+   b,[39m
[31m+   cmp,[39m
[31m+   same,[39m
[31m+ }: {[39m
[31m+   label: string[39m
[31m+   a: string | number[39m
[31m+   b: string | number[39m
[31m+   cmp?: [number, number][39m
[31m+   same?: boolean[39m
[31m+ }) {[39m
[31m+   const av = cmp ? cmp[0] : typeof a === 'number' ? a : 0[39m
[31m+   const bv = cmp ? cmp[1] : typeof b === 'number' ? b : 0[39m
[31m+   const aWin = !same && av > bv[39m
[31m+   const bWin = !same && bv > av[39m
[31m+   return ([39m
[31m+     <div className="flex items-center justify-between gap-2 border-b border-ink-800/50 py-2 text-sm last:border-0">[39m
[31m+       <span className={`w-1/3 font-mono font-bold ${aWin ? 'text-gold-400' : 'text-fog-400'}`}>{a}</span>[39m
[31m+       <span className="text-[10px] uppercase tracking-wide text-fog-600">{label}</span>[39m
[31m+       <span className={`w-1/3 text-right font-mono font-bold ${bWin ? 'text-mint-400' : 'text-fog-400'}`}>{b}</span>[39m
[31m+     </div>[39m
[31m+   )[39m
[31m+ }[39m
[31m+[39m
[31m+ function Chip({[39m
[31m+   active,[39m
[31m+   onClick,[39m
[31m+   children,[39m
[31m+ }: {[39m
[31m+   active: boolean[39m
[31m+   onClick: () => void[39m
[31m+   children: React.ReactNode[39m
[31m+ }) {[39m
[31m+   return ([39m
[31m+     <button[39m
[31m+       onClick={onClick}[39m
[31m+       className={`shrink-0 rounded-full border px-3.5 py-1.5 text-xs font-semibold transition-colors ${[39m
[31m+         active[39m
[31m+           ? 'border-gold-500/60 bg-gold-500/15 text-gold-400'[39m
[31m+           : 'border-ink-700 bg-ink-800 text-fog-400 hover:text-fog-200'[39m
[31m+       }`}[39m
[31m+     >[39m
[31m+       {children}[39m
[31m+     </button>[39m
[31m+   )[39m
[31m+ }[39m
[31m+[39m

[36m [2m❯[22m src/cloudIdentityWiring.test.ts:[2m21:25[22m[39m
    [90m 19| [39m
    [90m 20| [39m  [34mit[39m([32m'surfaces invalid codes and immediately reloads after profile cha[39m…
    [90m 21| [39m    [34mexpect[39m(statsSource)[33m.[39m[34mtoContain[39m([32m'Clique-Code ungültig – in Einstellu[39m…
    [90m   | [39m                        [31m^[39m
    [90m 22| [39m    [34mexpect[39m(statsSource)[33m.[39m[34mtoContain[39m([32m'void reload()'[39m)
    [90m 23| [39m  })

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯[22m[39m

[2m Test Files [22m [1m[31m1 failed[39m[22m[2m | [22m[1m[32m15 passed[39m[22m[90m (16)[39m
[2m      Tests [22m [1m[31m1 failed[39m[22m[2m | [22m[1m[32m116 passed[39m[22m[90m (117)[39m
[2m   Start at [22m 09:00:06
[2m   Duration [22m 2.17s[2m (transform 646ms, setup 0ms, collect 1.12s, tests 263ms, environment 4ms, prepare 1.48s)[22m


::error file=/home/runner/work/zentausend-app/zentausend-app/src/cloudIdentityWiring.test.ts,title=src/cloudIdentityWiring.test.ts > cloud player identity wiring > surfaces invalid codes and immediately reloads after profile changes,line=21,column=25::AssertionError: expected 'import { useEffect, useMemo, useRef, …' to contain 'Clique-Code ungültig – in Einstellung…'%0A%0A- Expected%0A+ Received%0A%0A- Clique-Code ungültig – in Einstellungen erneuern%0A+ import { useEffect, useMemo, useRef, useState } from 'react'%0A+ import {%0A+   aggregateStats,%0A+   clearHistoryIntegrityReport,%0A+   computeAwards,%0A+   computeForm,%0A+   computeHeadToHead,%0A+   computeNemesis,%0A+   getEvents,%0A+   getHistory,%0A+   getHistoryIntegrityReport,%0A+ } from '../lib/storage'%0A+ import { deleteGame, editGameEvent, pendingEventEditCount, syncAndMerge } from '../lib/cloud'%0A+ import { exportBackup, exportIntegrityReport, importBackup } from '../lib/backup'%0A+ import { cloudEnabled } from '../lib/supabase'%0A+ import type { GameRecord, PlayerStats } from '../lib/types'%0A+ import { playerColor } from '../lib/colors'%0A+ import { IconBack, IconChart, IconPencil, IconTrash, IconTrophy } from './Icons'%0A+ import { SettingsModal } from './SettingsModal'%0A+ import { AnalysisScreen } from './AnalysisScreen'%0A+ import { PlayerManager } from './PlayerManager'%0A+%0A+ const fmt = (n: number) => n.toLocaleString('de-DE')%0A+%0A+ export function StatsScreen({ onBack }: { onBack: () => void }) {%0A+   // Lokale Spiele sofort zeigen (offline-first): Die Cloud-Synchronisation läuft%0A+   // im Hintergrund und ersetzt die Liste nur, wenn sie erfolgreich war. So sieht%0A+   // man auf See (kein/schwaches Netz) seine Tabelle ohne Wartezeit.%0A+   const [games, setGames] = useState<GameRecord[]>(() => getHistory())%0A+   const [loading, setLoading] = useState(true)%0A+   const [online, setOnline] = useState(false)%0A+   const [codeDenied, setCodeDenied] = useState(false)%0A+   const [pendingSync, setPendingSync] = useState(() => pendingEventEditCount())%0A+   const [cloudCount, setCloudCount] = useState<number | null>(null)%0A+   const [integrity, setIntegrity] = useState(() => getHistoryIntegrityReport())%0A+   const [filter, setFilter] = useState<string>('')%0A+   const [busyId, setBusyId] = useState<number | null>(null)%0A+   const [showSettings, setShowSettings] = useState(false)%0A+   const [showPlayers, setShowPlayers] = useState(false)%0A+   const [focusAdmin, setFocusAdmin] = useState(false)%0A+   const [analysisGame, setAnalysisGame] = useState<GameRecord | null>(null)%0A+   // Nachträglich den Anlass eines Spiels bearbeiten (z. B. vergessen zu setzen%0A+   // oder Tippfehler korrigieren) – hält Spiel + Eingabefeld-Wert getrennt vom%0A+   // eigentlichen Spiel-Objekt, bis gespeichert wird.%0A+   const [editingGame, setEditingGame] = useState<GameRecord | null>(null)%0A+   const [editValue, setEditValue] = useState('')%0A+   const [msg, setMsg] = useState('')%0A+   const fileInput = useRef<HTMLInputElement>(null)%0A+%0A+   const reload = () => {%0A+     setLoading(true)%0A+     return syncAndMerge().then((res) => {%0A+       setGames(res.games)%0A+       setOnline(res.online)%0A+       setPendingSync(res.pending)%0A+       setCloudCount(res.cloudCount)%0A+       setCodeDenied(res.codeDenied)%0A+       if (res.identityConflicts > 0) {%0A+         setMsg(`${res.identityConflicts} Spieler-Zuordnungs-Konflikt${res.identityConflicts === 1 ? '' : 'e'} lokal gelöst.`)%0A+       }%0A+       setIntegrity(getHistoryIntegrityReport())%0A+       setLoading(false)%0A+     })%0A+   }%0A+%0A+   useEffect(() => {%0A+     let alive = true%0A+     setLoading(true)%0A+     syncAndMerge().then((res) => {%0A+       if (!alive) return%0A+       setGames(res.games)%0A+       setOnline(res.online)%0A+       setPendingSync(res.pending)%0A+       setCloudCount(res.cloudCount)%0A+       setCodeDenied(res.codeDenied)%0A+       if (res.identityConflicts > 0) {%0A+         setMsg(`${res.identityConflicts} Spieler-Zuordnungs-Konflikt${res.identityConflicts === 1 ? '' : 'e'} lokal gelöst.`)%0A+       }%0A+       setIntegrity(getHistoryIntegrityReport())%0A+       setLoading(false)%0A+     })%0A+     return () => {%0A+       alive = false%0A+     }%0A+   }, [])%0A+%0A+   const flash = (m: string) => {%0A+     setMsg(m)%0A+     window.setTimeout(() => setMsg((x) => (x === m ? '' : x)), 2600)%0A+   }%0A+%0A+   const handleImport = async (file: File) => {%0A+     try {%0A+       const res = await importBackup(file)%0A+       await reload()%0A+       const notes = [`${res.added} neu importiert`, `${res.total} gesamt`]%0A+       if (res.repaired > 0) notes.push(`${res.repaired} repariert`)%0A+       if (res.quarantined > 0) notes.push(`${res.quarantined} in Quarantäne`)%0A+       if (res.identitiesImported > 0) notes.push(`${res.identitiesImported} Spieler-Zuordnungen übernommen`)%0A+       if (res.identityConflicts > 0) notes.push(`${res.identityConflicts} Zuordnungs-Konflikte ausgelassen`)%0A+       setIntegrity(getHistoryIntegrityReport())%0A+       flash(notes.join(' · '))%0A+     } catch (e) {%0A+       flash(e instanceof Error ? e.message : 'Import fehlgeschlagen.')%0A+     }%0A+   }%0A+%0A+   const events = useMemo(() => getEvents(games), [games])%0A+   const stats = useMemo(() => aggregateStats(games, filter || undefined), [games, filter])%0A+   const awards = useMemo(() => computeAwards(games, filter || undefined), [games, filter])%0A+   const form = useMemo(() => computeForm(games, filter || undefined), [games, filter])%0A+   const filtered = useMemo(%0A+     () => (filter ? games.filter((g) => g.event === filter) : games),%0A+     [games, filter],%0A+   )%0A+%0A+   const handleDelete = async (g: GameRecord) => {%0A+     if (!window.confirm(`Spiel vom ${new Date(g.date).toLocaleDateString('de-DE')} (Sieger: ${g.winner}) löschen?`))%0A+       return%0A+     setBusyId(g.id)%0A+     const res = await deleteGame(g)%0A+     setBusyId(null)%0A+     if (res === 'offline') {%0A+       flash('Offline – Spiel wurde nicht gelöscht.')%0A+       return%0A+     }%0A+     if (res === 'denied') {%0A+       flash('Löschen nur mit Admin-Code.')%0A+       setFocusAdmin(true)%0A+       setShowSettings(true)%0A+       return%0A+     }%0A+     setGames((prev) => prev.filter((x) => x.id !== g.id))%0A+   }%0A+%0A+   // Sofort/optimistisch: lokal steht der neue Anlass schon fest, der Cloud-%0A+   // Abgleich läuft unabhängig im Hintergrund weiter (siehe editGameEvent) –%0A+   // die Bearbeitung soll nie auf ein schwaches Netz warten müssen.%0A+   const handleSaveEvent = () => {%0A+     if (!editingGame) return%0A+     const trimmed = editValue.trim()%0A+     const g = editingGame%0A+     setGames((prev) => prev.map((x) => (x.id === g.id ? { ...x, event: trimmed } : x)))%0A+     setEditingGame(null)%0A+     const sync = editGameEvent(g, trimmed)%0A+     setPendingSync(pendingEventEditCount())%0A+     flash('Anlass lokal gespeichert · Sync läuft…')%0A+     void sync.then((result) => {%0A+       setPendingSync(pendingEventEditCount())%0A+       if (result === 'ok') flash('Anlass synchronisiert.')%0A+       else if (result === 'denied') flash('Lokal gespeichert · Clique-Code prüfen.')%0A+       else flash('Lokal gespeichert · wird später synchronisiert.')%0A+     })%0A+   }%0A+%0A+   if (analysisGame) {%0A+     return <AnalysisScreen game={analysisGame} onBack={() => setAnalysisGame(null)} />%0A+   }%0A+%0A+   return (%0A+     <div className="mx-auto flex min-h-screen max-w-md flex-col px-4 pt-[max(env(safe-area-inset-top),1.25rem)] safe-pb">%0A+       {showSettings && (%0A+         <SettingsModal%0A+           focusAdmin={focusAdmin}%0A+           onClose={() => {%0A+             setShowSettings(false)%0A+             setFocusAdmin(false)%0A+           }}%0A+         />%0A+       )}%0A+%0A+       {showPlayers && (%0A+         <PlayerManager%0A+           games={games}%0A+           players={stats}%0A+           onClose={() => setShowPlayers(false)}%0A+           onChanged={(message) => {%0A+             setGames(getHistory())%0A+             setShowPlayers(false)%0A+             flash(message)%0A+             void reload()%0A+           }}%0A+         />%0A+       )}%0A+%0A+       {editingGame && (%0A+         <div%0A+           className="glass fixed inset-0 z-50 flex items-center justify-center p-6 animate-pop"%0A+           onClick={() => setEditingGame(null)}%0A+         >%0A+           <div%0A+             className="w-full max-w-sm rounded-3xl border border-ink-700 bg-ink-850 p-6 shadow-2xl"%0A+             onClick={(e) => e.stopPropagation()}%0A+           >%0A+             <h3 className="mb-1 text-lg font-black text-fog-100">Anlass bearbeiten</h3>%0A+             <p className="mb-4 text-xs text-fog-500">%0A+               {new Date(editingGame.date).toLocaleDateString('de-DE')} · Sieger: {editingGame.winner}%0A+             </p>%0A+             <input%0A+               type="text"%0A+               autoFocus%0A+               value={editValue}%0A+               onChange={(e) => setEditValue(e.target.value)}%0A+               onKeyDown={(e) => e.key === 'Enter' && handleSaveEvent()}%0A+               placeholder="Anlass, z. B. Skiurlaub 2025"%0A+               className="w-full rounded-xl border border-ink-700 bg-ink-950/60 px-4 py-3 text-fog-100 placeholder:text-fog-600 transition-colors focus:border-gold-500/70 focus:outline-none"%0A+             />%0A+             {events.length > 0 && (%0A+               <div className="mt-2.5 flex flex-wrap gap-2">%0A+                 {events.map((e) => (%0A+                   <button%0A+                     key={e}%0A+                     onClick={() => setEditValue(e)}%0A+                     className="rounded-full border border-ink-700 bg-ink-800 px-3 py-1 text-xs text-fog-400 transition-colors hover:border-gold-500/50 hover:text-gold-400"%0A+                   >%0A+                     {e}%0A+                   </button>%0A+                 ))}%0A+               </div>%0A+             )}%0A+             <div className="mt-5 grid grid-cols-2 gap-3">%0A+               <button%0A+                 onClick={() => setEditingGame(null)}%0A+                 className="rounded-2xl border border-ink-700 bg-ink-800 py-3 font-bold text-fog-200 transition-colors hover:text-fog-100"%0A+               >%0A+                 Abbrechen%0A+               </button>%0A+               <button%0A+                 onClick={handleSaveEvent}%0A+                 className="rounded-2xl bg-gradient-to-b from-mint-400 to-mint-500 py-3 font-bold text-ink-950 shadow-lg transition-all active:scale-[0.98]"%0A+               >%0A+                 Speichern%0A+               </button>%0A+             </div>%0A+           </div>%0A+         </div>%0A+       )}%0A+%0A+       <input%0A+         ref={fileInput}%0A+         type="file"%0A+         accept="application/json,.json"%0A+         className="hidden"%0A+         onChange={(e) => {%0A+           const f = e.target.files?.[0]%0A+           if (f) void handleImport(f)%0A+           e.target.value = ''%0A+         }}%0A+       />%0A+%0A+       <header className="mb-4 mt-2 flex items-center justify-between">%0A+         <h1 className="flex items-center gap-2 text-2xl font-black text-fog-100">%0A+           <IconChart className="h-6 w-6 text-gold-500" /> Statistik%0A+         </h1>%0A+         <button%0A+           onClick={onBack}%0A+           className="flex items-center gap-1.5 rounded-xl border border-ink-700 bg-ink-800/70 px-3 py-2 text-sm font-semibold text-fog-300 transition-colors hover:text-fog-100"%0A+         >%0A+           <IconBack className="h-4 w-4" /> Zurück%0A+         </button>%0A+       </header>%0A+%0A+       {/* Daten-Werkzeuge: Backup und ausdrücklich kontrollierte Spieler-Zuordnung. */}%0A+       <div className="mb-4 grid grid-cols-2 gap-2">%0A+         <button%0A+           onClick={() => exportBackup(games)}%0A+           disabled={games.length === 0}%0A+           className="flex-1 rounded-xl border border-ink-700 bg-ink-800/70 px-3 py-2 text-xs font-semibold text-fog-300 transition-colors hover:text-fog-100 disabled:opacity-40"%0A+         >%0A+           ⬇︎ Backup sichern%0A+         </button>%0A+         <button%0A+           onClick={() => fileInput.current?.click()}%0A+           className="rounded-xl border border-ink-700 bg-ink-800/70 px-3 py-2 text-xs font-semibold text-fog-300 transition-colors hover:text-fog-100"%0A+         >%0A+           ⬆︎ Backup laden%0A+         </button>%0A+         <button%0A+           type="button"%0A+           onClick={() => setShowPlayers(true)}%0A+           disabled={stats.length === 0}%0A+           className="col-span-2 rounded-xl border border-gold-500/30 bg-gold-500/10 px-3 py-2.5 text-xs font-bold text-gold-300 transition-colors disabled:opacity-40"%0A+         >%0A+           👥 Spielerprofile verwalten%0A+         </button>%0A+       </div>%0A+%0A+       {msg && (%0A+         <div className="mb-4 rounded-xl border border-gold-500/40 bg-gold-500/10 px-3 py-2 text-center text-xs font-semibold text-gold-300 animate-pop">%0A+           {msg}%0A+         </div>%0A+       )}%0A+%0A+       {integrity && (integrity.repaired > 0 || integrity.quarantined > 0) && (%0A+         <div className="mb-4 rounded-2xl border border-gold-500/40 bg-gold-500/10 p-3 text-xs text-fog-300">%0A+           <div className="font-bold text-gold-300">Datenprüfung abgeschlossen</div>%0A+           <div className="mt-1 leading-relaxed">%0A+             {integrity.repaired > 0 && <span>{integrity.repaired} repariert</span>}%0A+             {integrity.repaired > 0 && integrity.quarantined > 0 && <span> · </span>}%0A+             {integrity.quarantined > 0 && <span>{integrity.quarantined} sicher isoliert</span>}%0A+             {integrity.recoverySaved && <span> · Originalstand gesichert</span>}%0A+           </div>%0A+           {!integrity.quarantineStored && (%0A+             <div className="mt-1 font-semibold text-coral-400">Quarantäne konnte wegen Gerätespeicher nicht vollständig geschrieben werden.</div>%0A+           )}%0A+           <div className="mt-3 grid grid-cols-2 gap-2">%0A+             <button%0A+               type="button"%0A+               onClick={exportIntegrityReport}%0A+               className="rounded-xl border border-gold-500/30 bg-ink-900/60 px-2 py-2 font-semibold text-gold-300"%0A+             >%0A+               Prüfbericht sichern%0A+             </button>%0A+             <button%0A+               type="button"%0A+               onClick={() => {%0A+                 clearHistoryIntegrityReport()%0A+                 setIntegrity(null)%0A+               }}%0A+               className="rounded-xl border border-ink-700 bg-ink-900/60 px-2 py-2 font-semibold text-fog-400"%0A+             >%0A+               Hinweis ausblenden%0A+             </button>%0A+           </div>%0A+         </div>%0A+       )}%0A+%0A+       {/* Familienfreundlicher Sicherungsstatus mit explizitem manuellen Abgleich. */}%0A+       <div%0A+         className={`mb-4 rounded-2xl border p-3 ${%0A+           codeDenied%0A+             ? 'border-coral-500/40 bg-coral-500/10'%0A+             : pendingSync > 0%0A+               ? 'border-gold-500/40 bg-gold-500/10'%0A+               : online%0A+                 ? 'border-mint-500/30 bg-mint-500/10'%0A+                 : 'border-ink-700 bg-ink-900/40'%0A+         }`}%0A+         aria-live="polite"%0A+       >%0A+         <div className="flex items-start justify-between gap-3">%0A+           <div className="min-w-0 flex-1">%0A+             <div className="flex items-center gap-2">%0A+               <span%0A+                 className={`inline-block h-2.5 w-2.5 shrink-0 rounded-full ${%0A+                   loading%0A+                     ? 'animate-pulse bg-gold-500'%0A+                     : codeDenied%0A+                       ? 'bg-coral-400'%0A+                       : pendingSync > 0%0A+                         ? 'bg-gold-500'%0A+                         : online%0A+                           ? 'bg-mint-400'%0A+                           : 'bg-fog-600'%0A+                 }`}%0A+               />%0A+               <span className="font-bold text-fog-100">%0A+                 {loading%0A+                   ? 'Sicherung läuft…'%0A+                   : codeDenied%0A+                     ? 'Crew-Code prüfen'%0A+                     : pendingSync > 0%0A+                       ? 'Noch nicht alles gesichert'%0A+                       : online%0A+                         ? 'Alles gesichert'%0A+                         : cloudEnabled%0A+                           ? 'Gerade offline'%0A+                           : 'Nur auf diesem Gerät'}%0A+               </span>%0A+             </div>%0A+             <div className="mt-1 text-[11px] text-fog-400">%0A+               {games.length} {games.length === 1 ? 'Spiel' : 'Spiele'} auf diesem Gerät%0A+               {cloudCount !== null && <> · {cloudCount} in der Cloud</>}%0A+             </div>%0A+             <div className="mt-1 text-[11px] leading-relaxed text-fog-500">%0A+               {loading%0A+                 ? 'Lokale und gemeinsame Spielstände werden abgeglichen.'%0A+                 : codeDenied%0A+                   ? 'Der gespeicherte Crew-Code stimmt nicht. Deine lokalen Spiele bleiben erhalten.'%0A+                   : pendingSync > 0%0A+                     ? `${pendingSync} ${pendingSync === 1 ? 'Änderung ist' : 'Änderungen sind'} noch nur auf diesem Gerät.`%0A+                     : online%0A+                       ? 'Eure Spielstände sind auf allen Geräten gleich.'%0A+                       : cloudEnabled%0A+                         ? 'Sobald wieder Internet da ist, kannst du erneut sichern.'%0A+                         : 'Cloud-Sicherung ist auf diesem Gerät nicht eingerichtet.'}%0A+             </div>%0A+           </div>%0A+           <button%0A+             type="button"%0A+             onClick={() => void reload()}%0A+             disabled={loading}%0A+             aria-label="Jetzt sichern"%0A+             className="shrink-0 rounded-xl border border-ink-600 bg-ink-800 px-3 py-2 text-xs font-bold text-fog-200 transition-colors disabled:opacity-50"%0A+           >%0A+             {loading ? 'Läuft…' : 'Jetzt sichern'}%0A+           </button>%0A+         </div>%0A+         {codeDenied && (%0A+           <button%0A+             type="button"%0A+             onClick={() => setShowSettings(true)}%0A+             className="mt-3 w-full rounded-xl bg-coral-500/20 px-3 py-2 text-xs font-bold text-coral-300"%0A+           >%0A+             Crew-Code ändern%0A+           </button>%0A+         )}%0A+       </div>%0A+%0A+       {events.length > 0 && (%0A+         <div className="scrollbar-hide mb-5 flex gap-2 overflow-x-auto whitespace-nowrap pb-1">%0A+           <Chip active={filter === ''} onClick={() => setFilter('')}>%0A+             Alle%0A+           </Chip>%0A+           {events.map((e) => (%0A+             <Chip key={e} active={filter === e} onClick={() => setFilter(e)}>%0A+               {e}%0A+             </Chip>%0A+           ))}%0A+         </div>%0A+       )}%0A+%0A+       {loading && games.length === 0 ? (%0A+         <div className="grid flex-1 place-items-center text-fog-600">Lade…</div>%0A+       ) : games.length === 0 ? (%0A+         <div className="grid flex-1 place-items-center text-center text-fog-600">%0A+           <div>%0A+             <IconTrophy className="mx-auto mb-3 h-10 w-10 text-ink-600" />%0A+             <p>Noch keine Spiele gespeichert.</p>%0A+             <p className="mt-1 text-sm">Spielt eine Runde – hier entsteht eure ewige Tabelle.</p>%0A+           </div>%0A+         </div>%0A+       ) : (%0A+         <>%0A+           {/* Event-Podest: nur wenn ein Anlass gefiltert ist */}%0A+           {filter !== '' && stats.length > 0 && <EventPodium event={filter} top={stats.slice(0, 3)} />}%0A+%0A+           {/* Awards & Rekorde */}%0A+           {awards.length > 0 && (%0A+             <section className="mb-6">%0A+               <h2 className="mb-2 text-xs font-bold uppercase tracking-widest text-fog-500">Awards & Rekorde</h2>%0A+               <div className="grid grid-cols-2 gap-2.5">%0A+                 {awards.map((a) => (%0A+                   <div key={a.key} className="rounded-2xl border border-ink-700/70 bg-ink-850/70 p-3">%0A+                     <div className="mb-1 flex items-center gap-2 text-[11px] font-bold uppercase tracking-wide text-fog-500">%0A+                       <span className="text-base">{a.emoji}</span> {a.title}%0A+                     </div>%0A+                     <div className="font-bold text-fog-100">{a.name}</div>%0A+                     <div className="text-xs text-gold-400">{a.detail}</div>%0A+                   </div>%0A+                 ))}%0A+               </div>%0A+             </section>%0A+           )}%0A+%0A+           {/* Aktuelle Form – wer läuft gerade heiß? (letzte 5 Spiele, neuestes links) */}%0A+           {form.length > 0 && (%0A+             <section className="mb-6">%0A+               <h2 className="mb-2 text-xs font-bold uppercase tracking-widest text-fog-500">%0A+                 Aktuelle Form%0A+               </h2>%0A+               <div className="overflow-hidden rounded-2xl border border-ink-700/80 bg-ink-850/80">%0A+                 {form.map((f) => (%0A+                   <div%0A+                     key={f.id}%0A+                     className="flex items-center justify-between gap-3 border-b border-ink-800/60 px-4 py-2.5 last:border-0"%0A+                   >%0A+                     <span className="truncate text-sm font-semibold text-fog-100">{f.name}</span>%0A+                     <span className="flex shrink-0 items-center gap-1.5">%0A+                       {f.results.map((won, j) => (%0A+                         <span%0A+                           key={j}%0A+                           title={won ? 'Sieg' : 'verloren'}%0A+                           className={`grid h-5 w-5 place-items-center rounded-full text-[10px] font-black ${%0A+                             won%0A+                               ? 'bg-gold-500/20 text-gold-400'%0A+                               : 'bg-ink-800 text-fog-600'%0A+                           }`}%0A+                         >%0A+                           {won ? 'S' : '·'}%0A+                         </span>%0A+                       ))}%0A+                     </span>%0A+                   </div>%0A+                 ))}%0A+               </div>%0A+               <p className="mt-1.5 text-[10px] text-fog-600">Letzte Spiele · neuestes links · S = Sieg</p>%0A+             </section>%0A+           )}%0A+%0A+           {/* Duell – direkter Vergleich zweier Spieler */}%0A+           {stats.length >= 2 && (%0A+             <DuelSection players={stats.map((s) => ({ id: s.id, name: s.name }))} games={games} event={filter || undefined} />%0A+           )}%0A+%0A+           {/* Ewige Bestenliste */}%0A+           <section className="mb-6">%0A+             <h2 className="mb-2 text-xs font-bold uppercase tracking-widest text-fog-500">Ewige Bestenliste</h2>%0A+             <div className="overflow-hidden rounded-2xl border border-ink-700/80 bg-ink-850/80">%0A+               <div className="grid grid-cols-[1.6fr_0.6fr_0.6fr_0.9fr] gap-2 border-b border-ink-800 px-4 py-2 text-[10px] font-bold uppercase tracking-wider text-fog-600">%0A+                 <span>Spieler</span>%0A+                 <span className="text-right">Siege</span>%0A+                 <span className="text-right">Nieten</span>%0A+                 <span className="text-right">Bestwert</span>%0A+               </div>%0A+               {stats.map((s, i) => (%0A+                 <div%0A+                   key={s.id}%0A+                   className="grid grid-cols-[1.6fr_0.6fr_0.6fr_0.9fr] items-center gap-2 border-b border-ink-800/60 px-4 py-2.5 text-sm last:border-0"%0A+                 >%0A+                   <span className="flex items-center gap-2 font-semibold text-fog-100">%0A+                     <span className="w-4 shrink-0 text-center text-xs">%0A+                       {i === 0 ? '🥇' : i === 1 ? '🥈' : i === 2 ? '🥉' : <span className="text-fog-600">{i + 1}</span>}%0A+                     </span>%0A+                     {s.name}%0A+                     <span className="text-[10px] font-normal text-fog-600">{s.games} Sp.</span>%0A+                   </span>%0A+                   <span className="text-right font-mono font-bold text-gold-400">{s.wins}</span>%0A+                   <span className="text-right font-mono text-coral-400">{s.bustRate.toFixed(1)}</span>%0A+                   <span className="text-right font-mono text-fog-300">{fmt(s.bestScore)}</span>%0A+                 </div>%0A+               ))}%0A+             </div>%0A+           </section>%0A+%0A+           {/* Verlauf */}%0A+           <section className="space-y-2.5">%0A+             <h2 className="mb-1 text-xs font-bold uppercase tracking-widest text-fog-500">%0A+               Verlauf ({filtered.length})%0A+             </h2>%0A+             {filtered.map((g) => (%0A+               <div%0A+                 key={g.id}%0A+                 role="button"%0A+                 tabIndex={0}%0A+                 onClick={() => setAnalysisGame(g)}%0A+                 onKeyDown={(e) => {%0A+                   if (e.target !== e.currentTarget) return%0A+                   if (e.key === 'Enter' || e.key === ' ') {%0A+                     e.preventDefault()%0A+                     setAnalysisGame(g)%0A+                   }%0A+                 }}%0A+                 className="block w-full cursor-pointer rounded-2xl border border-ink-700/70 bg-ink-850/70 p-4 text-left transition-colors hover:border-ink-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold-500/60"%0A+               >%0A+                 <div className="mb-1.5 flex items-center justify-between">%0A+                   <div className="text-[11px] text-fog-500">%0A+                     {new Date(g.date).toLocaleDateString('de-DE')}%0A+                     {g.event && <span className="ml-2 text-gold-500/80">· {g.event}</span>}%0A+                   </div>%0A+                   <div className="flex items-center gap-2">%0A+                     <div className="flex items-center gap-1.5 text-sm font-bold text-gold-400">%0A+                       <IconTrophy className="h-3.5 w-3.5" /> {g.winner}%0A+                     </div>%0A+                     <button%0A+                       type="button"%0A+                       onClick={(e) => {%0A+                         e.stopPropagation()%0A+                         setEditValue(g.event ?? '')%0A+                         setEditingGame(g)%0A+                       }}%0A+                       className="p-1.5 text-fog-600 transition-colors hover:text-gold-400 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold-500/60"%0A+                       aria-label="Anlass bearbeiten"%0A+                     >%0A+                       <IconPencil className="h-4 w-4" />%0A+                     </button>%0A+                     <button%0A+                       type="button"%0A+                       disabled={busyId === g.id}%0A+                       onClick={(e) => {%0A+                         e.stopPropagation()%0A+                         void handleDelete(g)%0A+                       }}%0A+                       className={`p-1.5 text-fog-600 transition-colors hover:text-coral-400 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-coral-500/60 ${%0A+                         busyId === g.id ? 'opacity-40' : ''%0A+                       }`}%0A+                       aria-label="Spiel löschen"%0A+                     >%0A+                       <IconTrash className="h-4 w-4" />%0A+                     </button>%0A+                   </div>%0A+                 </div>%0A+                 <div className="flex flex-wrap items-center gap-x-4 gap-y-0.5 text-xs text-fog-400">%0A+                   {[...g.players]%0A+                     .sort((a, b) => b.score - a.score)%0A+                     .map((pl) => (%0A+                       <span key={pl.name}>%0A+                         {pl.name} <span className="font-mono text-fog-300">{fmt(pl.score)}</span>%0A+                       </span>%0A+                     ))}%0A+                   <span className="ml-auto text-[10px] text-fog-600">Analyse ›</span>%0A+                 </div>%0A+               </div>%0A+             ))}%0A+           </section>%0A+         </>%0A+       )}%0A+     </div>%0A+   )%0A+ }%0A+%0A+ function EventPodium({ event, top }: { event: string; top: PlayerStats[] }) {%0A+   // Reihenfolge auf dem Treppchen: 2. links, 1. Mitte (höchste Stufe), 3. rechts.%0A+   const steps = [%0A+     { p: top[1], rank: 2, medal: '🥈', h: 'h-16' },%0A+     { p: top[0], rank: 1, medal: '🥇', h: 'h-24' },%0A+     { p: top[2], rank: 3, medal: '🥉', h: 'h-11' },%0A+   ].filter((s) => s.p)%0A+%0A+   return (%0A+     <section className="mb-6">%0A+       <h2 className="mb-3 text-xs font-bold uppercase tracking-widest text-fog-500">%0A+         🏆 {event} – Podest%0A+       </h2>%0A+       <div className="flex items-end justify-center gap-2 rounded-2xl border border-ink-700/70 bg-ink-850/60 px-3 pt-3">%0A+         {steps.map(({ p, rank, medal, h }) => (%0A+           <div key={rank} className="flex w-1/3 max-w-[120px] flex-col items-center">%0A+             <span className="text-2xl">{medal}</span>%0A+             <span className="max-w-full truncate text-sm font-bold text-fog-100">{p!.name}</span>%0A+             <span className="mb-1 text-[11px] font-semibold text-gold-400">%0A+               {p!.wins} {p!.wins === 1 ? 'Sieg' : 'Siege'}%0A+             </span>%0A+             <div%0A+               className={`grid w-full place-items-center rounded-t-xl ${h}`}%0A+               style={{ background: `linear-gradient(to top, ${playerColor(p!.name)}22, ${playerColor(p!.name)}66)` }}%0A+             >%0A+               <span className="font-display text-2xl font-black text-fog-100">{rank}</span>%0A+             </div>%0A+           </div>%0A+         ))}%0A+       </div>%0A+     </section>%0A+   )%0A+ }%0A+%0A+ function DuelSection({%0A+   players,%0A+   games,%0A+   event,%0A+ }: {%0A+   players: { id: string; name: string }[]%0A+   games: GameRecord[]%0A+   event?: string%0A+ }) {%0A+   const [a, setA] = useState(players[0]?.id ?? '')%0A+   const [b, setB] = useState(players[1]?.id ?? '')%0A+%0A+   const aId = players.some((player) => player.id === a) ? a : players[0]?.id ?? ''%0A+   const bId =%0A+     players.some((player) => player.id === b) && b !== aId%0A+       ? b%0A+       : players.find((player) => player.id !== aId)?.id ?? ''%0A+   const aPlayer = players.find((player) => player.id === aId)%0A+   const bPlayer = players.find((player) => player.id === bId)%0A+%0A+   const h = useMemo(() => computeHeadToHead(aId, bId, games, event), [aId, bId, games, event])%0A+   const nemA = useMemo(() => computeNemesis(aId, games, event), [aId, games, event])%0A+   const nemB = useMemo(() => computeNemesis(bId, games, event), [bId, games, event])%0A+%0A+   if (!aPlayer || !bPlayer) return null%0A+   const aheadTotal = h.aAhead + h.bAhead%0A+   const aPct = aheadTotal ? Math.round((h.aAhead / aheadTotal) * 100) : 50%0A+%0A+   return (%0A+     <section className="mb-6">%0A+       <h2 className="mb-2 text-xs font-bold uppercase tracking-widest text-fog-500">Duell</h2>%0A+       <div className="rounded-2xl border border-ink-700/80 bg-ink-850/80 p-4">%0A+         <div className="mb-4 grid grid-cols-[1fr_auto_1fr] items-center gap-2">%0A+           <select%0A+             value={aId}%0A+             onChange={(e) => setA(e.target.value)}%0A+             className="w-full rounded-xl border border-ink-700 bg-ink-900 px-2.5 py-2 text-sm font-bold text-gold-400"%0A+           >%0A+             {players.map((player) => (%0A+               <option key={player.id} value={player.id} disabled={player.id === bId}>%0A+                 {player.name}%0A+               </option>%0A+             ))}%0A+           </select>%0A+           <span className="text-[11px] font-black text-fog-600">vs</span>%0A+           <select%0A+             value={bId}%0A+             onChange={(e) => setB(e.target.value)}%0A+             className="w-full rounded-xl border border-ink-700 bg-ink-900 px-2.5 py-2 text-right text-sm font-bold text-mint-400"%0A+           >%0A+             {players.map((player) => (%0A+               <option key={player.id} value={player.id} disabled={player.id === aId}>%0A+                 {player.name}%0A+               </option>%0A+             ))}%0A+           </select>%0A+         </div>%0A+%0A+         {h.games === 0 ? (%0A+           <p className="py-3 text-center text-xs text-fog-500">Noch kein gemeinsames Spiel.</p>%0A+         ) : (%0A+           <>%0A+             <div className="mb-1 flex items-center justify-between text-xs font-bold">%0A+               <span className="text-gold-400">{h.aAhead}×</span>%0A+               <span className="text-fog-600">öfter vorn</span>%0A+               <span className="text-mint-400">{h.bAhead}×</span>%0A+             </div>%0A+             <div className="mb-4 flex h-2.5 overflow-hidden rounded-full bg-ink-900">%0A+               <div className="bg-gold-500/70" style={{ width: `${aPct}%25` }} />%0A+               <div className="bg-mint-400/70" style={{ width: `${100 - aPct}%25` }} />%0A+             </div>%0A+%0A+             <DuelRow label="Spiele zusammen" a={h.games} b={h.games} same />%0A+             <DuelRow label="Siege" a={h.aWins} b={h.bWins} />%0A+             <DuelRow label="Bestwert" a={fmt(h.aBest)} b={fmt(h.bBest)} cmp={[h.aBest, h.bBest]} />%0A+             <DuelRow label="Ø Punkte" a={fmt(h.aAvg)} b={fmt(h.bAvg)} cmp={[h.aAvg, h.bAvg]} />%0A+%0A+             {(nemA || nemB) && (%0A+               <div className="mt-3 border-t border-ink-800 pt-3 text-[11px] text-fog-500">%0A+                 {nemA && (%0A+                   <p>%0A+                     😨 <span className="font-semibold text-fog-300">{aPlayer.name}</span>s Angstgegner:{' '}%0A+                     <span className="font-semibold text-coral-400">{nemA.name}</span> ({nemA.ahead}/{nemA.of} vorn)%0A+                   </p>%0A+                 )}%0A+                 {nemB && (%0A+                   <p className="mt-0.5">%0A+                     😨 <span className="font-semibold text-fog-300">{bPlayer.name}</span>s Angstgegner:{' '}%0A+                     <span className="font-semibold text-coral-400">{nemB.name}</span> ({nemB.ahead}/{nemB.of} vorn)%0A+                   </p>%0A+                 )}%0A+               </div>%0A+             )}%0A+           </>%0A+         )}%0A+       </div>%0A+     </section>%0A+   )%0A+ }%0A+%0A+ function DuelRow({%0A+   label,%0A+   a,%0A+   b,%0A+   cmp,%0A+   same,%0A+ }: {%0A+   label: string%0A+   a: string | number%0A+   b: string | number%0A+   cmp?: [number, number]%0A+   same?: boolean%0A+ }) {%0A+   const av = cmp ? cmp[0] : typeof a === 'number' ? a : 0%0A+   const bv = cmp ? cmp[1] : typeof b === 'number' ? b : 0%0A+   const aWin = !same && av > bv%0A+   const bWin = !same && bv > av%0A+   return (%0A+     <div className="flex items-center justify-between gap-2 border-b border-ink-800/50 py-2 text-sm last:border-0">%0A+       <span className={`w-1/3 font-mono font-bold ${aWin ? 'text-gold-400' : 'text-fog-400'}`}>{a}</span>%0A+       <span className="text-[10px] uppercase tracking-wide text-fog-600">{label}</span>%0A+       <span className={`w-1/3 text-right font-mono font-bold ${bWin ? 'text-mint-400' : 'text-fog-400'}`}>{b}</span>%0A+     </div>%0A+   )%0A+ }%0A+%0A+ function Chip({%0A+   active,%0A+   onClick,%0A+   children,%0A+ }: {%0A+   active: boolean%0A+   onClick: () => void%0A+   children: React.ReactNode%0A+ }) {%0A+   return (%0A+     <button%0A+       onClick={onClick}%0A+       className={`shrink-0 rounded-full border px-3.5 py-1.5 text-xs font-semibold transition-colors ${%0A+         active%0A+           ? 'border-gold-500/60 bg-gold-500/15 text-gold-400'%0A+           : 'border-ink-700 bg-ink-800 text-fog-400 hover:text-fog-200'%0A+       }`}%0A+     >%0A+       {children}%0A+     </button>%0A+   )%0A+ }%0A+%0A%0A ❯ src/cloudIdentityWiring.test.ts:21:25%0A%0A
```
